export {
  mergeProps,
  normalizeProps,
  portalStub,
  presenceStub,
  provideDirection,
  shouldRenderPresence,
  useDirection,
  useMachine,
  type DirectionProviderProps,
  type PortalProps,
  type PresenceProps,
  type SveltePropTypes,
} from "@kimak/headless-svelte";

// Component sugar (Button, Checkbox, Dialog) lands after headless ports.
