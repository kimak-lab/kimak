export {
  DirectionProvider,
  mergeProps,
  normalizeProps,
  Portal,
  Presence,
  useDirection,
  useMachine,
  type VuePropTypes,
} from "@kimak/headless-vue";

// Component sugar (Button, Checkbox, Dialog) lands after headless ports.
